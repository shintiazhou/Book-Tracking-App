import React from "react";
import { styled } from "@mui/material/styles";

function BookDetails() {
  return <Container>this is book details</Container>;
}
const Container = styled("div")(({ theme }) => ({}));
export default BookDetails;
